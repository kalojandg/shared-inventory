// ─── BASE DETAIL ROUTING (lane bases-detail — task 630) ─────
// Hash routing (#base/<id>) + the editable name/location/history fields live
// here. This foundation task ships no-op stubs with the EXACT export
// signatures the app.js wiring expects; task 630 fills the bodies.
function renderRoute() {}
function openBaseDetail() {}
function saveBaseDetail() {}

export { renderRoute, openBaseDetail, saveBaseDetail };
