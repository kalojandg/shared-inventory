// ─── BASE SUB-TABLES (lane bases-tables — task 640) ─────────
// The three sub-tables (buildings / populace / production) with their modal,
// accordion and drag ordering live here. This foundation task ships no-op
// stubs with the EXACT export signatures the app.js wiring expects; task 640
// fills the bodies. It listens to the `base-route` event from base-detail.js.
function renderSubTables() {}
function openSubModal() {}
function closeSubModal() {}
function editSub() {}
function saveSub() {}
function deleteSub() {}

export { renderSubTables, openSubModal, closeSubModal, editSub, saveSub, deleteSub };
